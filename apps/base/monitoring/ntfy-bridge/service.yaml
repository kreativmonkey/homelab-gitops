apiVersion: v1
kind: Service
metadata:
  name: ntfy-bridge
  namespace: monitoring
  labels:
    app.kubernetes.io/name: ntfy-bridge
    app.kubernetes.io/part-of: homelab-monitoring
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ntfy-bridge
  ports:
    - name: http
      port: 8080
      targetPort: http
