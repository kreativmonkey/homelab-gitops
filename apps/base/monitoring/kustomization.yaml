apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - vm-k8s-stack
  - victoria-logs
  - notifications
  - ntfy-bridge
  # Kite-Prometheus-Bridge: mapped 8429→8481 (vmselect metrics), Label app.kubernetes.io/name: vmselect
  # Kite's auto-discovery braucht Port 8429, vmselect läuft auf 8481
  - kite-prometheus-discovery.yaml
  # VMRule CRDs: separate Flux Kustomization apps-monitoring-rules (after vm operator)
