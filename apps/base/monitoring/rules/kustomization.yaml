apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - platform-p0-vmrule.yaml
  - workload-remediation-vmrule.yaml
  - blackbox-vmrule.yaml
  - truenas-vmrule.yaml
  - iscsi-storage-vmrule.yaml
  # authentik-vmrule.yaml entfaellt: die beiden Blueprint-Check-Alerts sind im
  # generischen Job-Watchdog aufgegangen (Labels am CronJob statt eigener Regeln).
  - job-watchdog-vmrule.yaml
  - dead-mans-switch-vmrule.yaml
