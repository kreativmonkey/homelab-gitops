# OAuth credentials for Grafana ↔ Authentik (application slug: grafana)
#
# From gitops-homelab/ with SOPS_AGE_KEY_FILE set:
#   just grafana-authentik-oauth
#
# Then sync the same client-secret into Authentik (Provider for Grafana) if the blueprint
# was already applied with the placeholder — or re-apply after editing the blueprint.
