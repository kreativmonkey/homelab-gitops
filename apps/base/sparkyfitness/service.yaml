apiVersion: v1
kind: Service
metadata:
  name: sparkyfitness
  namespace: sparkyfitness
spec:
  selector:
    app: sparkyfitness
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
