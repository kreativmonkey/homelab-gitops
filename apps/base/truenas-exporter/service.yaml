apiVersion: v1
kind: Service
metadata:
  name: truenas-exporter
  namespace: monitoring
  labels:
    app: truenas-exporter
spec:
  selector:
    app: truenas-exporter
  ports:
    - name: metrics
      port: 9849
      targetPort: metrics
      protocol: TCP
  type: ClusterIP
