apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tandoor-static
  namespace: tandoor
  # Preserve the legacy rollback PVC if it is ever removed from the app catalog.
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-media-static
  volumeName: pv-nfs-tandoor-static
  resources:
    requests:
      storage: 5Gi
---
# Migration target; workload switches only after verified copy from legacy NFS.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tandoor-media-iscsi
  namespace: tandoor
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-iscsi
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tandoor-media
  namespace: tandoor
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-media-static
  volumeName: pv-nfs-tandoor-media
  resources:
    requests:
      storage: 50Gi
