# Linkwarden app secrets (SOPS).
#
#   cd apps/base/linkwarden
#   just sops-create linkwarden-app linkwarden \
#     NEXTAUTH_SECRET="$(openssl rand -base64 32)" \
#     MEILI_MASTER_KEY="$(openssl rand -base64 32)" \
#     AUTHENTIK_CLIENT_ID="..." \
#     AUTHENTIK_CLIENT_SECRET="..." \
#     OPENAI_API_KEY=""
#
# Then add linkwarden-app.secret.yaml to kustomization.yaml.
