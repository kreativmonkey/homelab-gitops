# SearXNG server secret (SOPS). Rotates the leaked key from the old ConfigMap.
#
#   cd apps/base/searxng
#   export SOPS_AGE_KEY_FILE=~/.config/sops/age/keys.txt
#   just -f ../../../justfile sops-create searxng-secret searxng \
#     SEARXNG_SECRET="$(openssl rand -hex 32)"
#
# Then add searxng-secret.secret.yaml to kustomization.yaml.
