apiVersion: v1
kind: Service
metadata:
  name: valkey
  namespace: searxng
spec:
  selector:
    app: valkey
  ports:
    - name: valkey
      port: 6379
      targetPort: valkey
      protocol: TCP
  type: ClusterIP
