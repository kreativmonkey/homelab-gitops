apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - nextcloud-exapps-namespace.yaml
  - nextcloud-admin.secret.yaml
  - nextcloud-authentik-oauth.secret.yaml
  - nextcloud-collab.secret.yaml
  - nextcloud-s3-credentials.secret.yaml
  - pvc.yaml
  # Bleibt vorerst stehen: Rueckweg, falls die local-path-Migration klemmt.
  # Nach ein paar stabilen Wochen entfernen (dann loescht reclaimPolicy die LUN).
  - pvc-iscsi.yaml
  - pvc-local.yaml
  - whiteboard-deployment.yaml
  - appapi-harp-rbac.yaml
  - appapi-harp-deployment.yaml
  - helmrelease.yaml
  - ingress.yaml
