apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-fotos
  namespace: immich
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-media-static
  volumeName: pv-nfs-immich-fotos
  resources:
    requests:
      storage: 2Ti
