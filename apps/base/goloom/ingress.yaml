apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: goloom
  namespace: goloom
  annotations:
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: Goloom
    gethomepage.dev/description: Goloom
    gethomepage.dev/group: Development
    gethomepage.dev/icon: goloom.png
    # TLS: wildcard-f4mily-net-tls via reflector (cert-manager namespace)
    nginx.org/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - goloom.f4mily.net
      secretName: wildcard-f4mily-net-tls
  rules:
    - host: goloom.f4mily.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: goloom
                port:
                  number: 80
