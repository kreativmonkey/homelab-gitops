apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: goloom-data
  namespace: goloom
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 4Gi
