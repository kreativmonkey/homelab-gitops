apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: homelab-postgres-immich
  namespace: immich
spec:
  name: immich
  owner: immich
  cluster:
    name: homelab-postgres
    namespace: cnpg-system
  extensions:
    - name: vector
      ensure: present
