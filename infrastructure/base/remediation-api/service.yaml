apiVersion: v1
kind: Service
metadata:
  name: remediation-api
  namespace: ai-ops
  labels:
    app.kubernetes.io/name: remediation-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: remediation-api
