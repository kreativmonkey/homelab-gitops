apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# searxng = metasearch. Ingress + same-ns valkey; queries upstream search
# engines over HTTPS. Baseline + external egress.
namespace: searxng
components:
  - ../baseline
  - ../egress-external
