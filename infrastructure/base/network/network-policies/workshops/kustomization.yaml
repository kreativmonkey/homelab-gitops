apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# workshops = static site. Ingress; git-sync clones https://git.f4mily.net.
# Baseline + external egress.
namespace: workshops
components:
  - ../baseline
  - ../egress-external
