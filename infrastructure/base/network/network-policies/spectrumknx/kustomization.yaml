apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# spectrumknx = KNX home automation. Ingress; tunnels to the KNX gateway at
# 192.168.10.20:3671 (TCP+UDP). Postgres runs same-namespace (timescaledb).
namespace: spectrumknx
components:
  - ../baseline
resources:
  - allow-egress-knx.yaml
