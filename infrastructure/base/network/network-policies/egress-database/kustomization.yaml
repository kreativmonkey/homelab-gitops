apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
# Egress from an app namespace to the central CNPG PostgreSQL tier. All
# clusters (homelab-postgres + per-app clusters) live in cnpg-system. Add to
# any namespace whose pods connect to postgres on 5432.
resources:
  - allow-egress-database.yaml
