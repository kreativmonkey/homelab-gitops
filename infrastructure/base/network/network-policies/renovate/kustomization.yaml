apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# renovate = dependency bot. Reaches the Forgejo (Gitea) API (forgejo NS
# :3000) and external registries (docker.io / ghcr / github).
namespace: renovate
components:
  - ../baseline
  - ../egress-external
resources:
  - allow-egress-forgejo.yaml
