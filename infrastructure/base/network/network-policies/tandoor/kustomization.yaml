apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# tandoor = recipe manager. Ingress; CNPG postgres
# (homelab-postgres-tandoor in cnpg-system); SMTP invites (465).
# Baseline + database + mail egress.
namespace: tandoor
components:
  - ../baseline
  - ../egress-database
  - ../egress-mail
