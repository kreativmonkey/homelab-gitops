apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# sparkyfitness = fitness tracker. Ingress; CNPG postgres
# (homelab-postgres-sparkyfitness in cnpg-system); OIDC via login.f4mily.net.
# Baseline + database + external egress.
namespace: sparkyfitness
components:
  - ../baseline
  - ../egress-database
  - ../egress-external
