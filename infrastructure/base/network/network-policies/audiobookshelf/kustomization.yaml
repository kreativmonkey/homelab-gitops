apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# audiobookshelf = media server. Static ingress; optional cover/metadata fetch
# needs outbound HTTPS. Baseline + external egress.
namespace: audiobookshelf
components:
  - ../baseline
  - ../egress-external
