apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# goloom = app. Ingress; CNPG postgres (homelab-postgres-goloom in
# cnpg-system); OIDC via login.f4mily.net. Baseline + database + external.
namespace: goloom
components:
  - ../baseline
  - ../egress-database
  - ../egress-external
