apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# immich = CNPG (postgres) + valkey (in-ns) + ML model download (ext 443).
namespace: immich
components:
  - ../baseline
  - ../egress-external
  - ../egress-database
