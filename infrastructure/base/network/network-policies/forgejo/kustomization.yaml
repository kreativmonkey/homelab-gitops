apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# forgejo = CNPG (postgres) + redis (in-ns) + external webhooks/avatars/OIDC.
namespace: forgejo
components:
  - ../baseline
  - ../egress-external
  - ../egress-database
