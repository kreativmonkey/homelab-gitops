apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: jetstack-repo
  namespace: flux-system
spec:
  interval: 24h
  url: https://charts.jetstack.io
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  interval: 1h
  chart:
    spec:
      chart: cert-manager
      version: "v1.14.4" # Zielversion für Renovate Bot
      sourceRef:
        kind: HelmRepository
        name: jetstack-repo
        namespace: flux-system
  values:
    installCRDs: true
    # PriorityClass für Systemkomponenten
    global:
      priorityClassName: "homelab-infrastructure"
    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        cpu: 200m
        memory: 256Mi
---
# 2. Hetzner Webhook Repository
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: hetzner-webhook-repo
  namespace: flux-system
spec:
  interval: 24h
  url: https://charts.hetzner.cloud
---
# 3. Hetzner Webhook Release
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager-webhook-hetzner
  namespace: cert-manager
spec:
  interval: 1h
  chart:
    spec:
      chart: cert-manager-webhook-hetzner
      version: "0.6.5" # Aktuelle Version, Ziel für Renovate Bot
      sourceRef:
        kind: HelmRepository
        name: hetzner-webhook-repo
        namespace: flux-system
  dependsOn:
    - name: cert-manager # Webhook startet erst, wenn cert-manager läuft