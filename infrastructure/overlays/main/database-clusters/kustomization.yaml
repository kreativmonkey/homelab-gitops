apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cluster.yaml
  - scheduled-backup.yaml
  # Enable after: just overlay-secrets-help (requires SOPS_AGE_KEY_FILE)
  # - barman-s3-credentials.secret.yaml
  - credentials
  # Velero volume-level restore (optional, separate from Barman PITR)
  - restore.yaml
