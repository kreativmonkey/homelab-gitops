apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - ingress.yaml
  # Enable after: just pgadmin-credentials admin@example.com 'your-password'
  # - pgadmin-credentials.secret.yaml
